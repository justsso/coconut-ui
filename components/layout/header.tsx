import React from "react";
let prefix = 'coconut-ui';

function Header(){
    return (
        <div className={`${prefix}-layout-header`}>
            Header
        </div>
    )
}
export default Header
